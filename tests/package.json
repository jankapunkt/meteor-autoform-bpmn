{
  "name": "tests",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test-watch-coverage": "COVERAGE=1 COVERAGE_APP_FOLDER=$(pwd)/ COVERAGE_VERBOSE=1 TEST_WATCH=1 TEST_SERVER=0 meteor test --driver-package meteortesting:mocha --port=4444",
    "test-watch": "METEOR_PACKAGE_DIRS=../ TEST_WATCH=1 TEST_SERVER=0 meteor test --driver-package meteortesting:mocha --port=4444",
    "test": "METEOR_PACKAGE_DIRS=../ TEST_SERVER=0 TEST_BROWSER_DRIVER=phantomjs meteor test --once --driver-package meteortesting:mocha --port=4444"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.42",
    "meteor-node-stubs": "^0.3.2",
    "simpl-schema": "1.4.2"
  },
  "devDependencies": {
    "chromedriver": "^2.37.0",
    "phantomjs-prebuilt": "^2.1.16",
    "selenium-webdriver": "^4.0.0-alpha.1"
  }
}
