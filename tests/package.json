{
  "name": "tests",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test-watch": "METEOR_PACKAGE_DIRS=../ TEST_WATCH=1 TEST_SERVER=0 meteor test --driver-package meteortesting:mocha --port=4444",
    "test": "METEOR_PACKAGE_DIRS=../ TEST_SERVER=0 meteor test-packages ./ --driver-package meteortesting:mocha --port=4444"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.42",
    "meteor-node-stubs": "^0.3.2",
    "simpl-schema": "1.4.2"
  }
}
